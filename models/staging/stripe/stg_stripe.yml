version: 2

models:
  - name: stg_payments
    columns:
      - name: order_id
        tests:
          - not_null
      - name: payment_id
        tests:
          - not_null
          - unique
      - name: status
        test:
          - accepted_values:
              values:
                - success
                - fail
        